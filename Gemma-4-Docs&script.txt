Let's try this again. This is Gemma-4-26b-a4b.

5 Days ago I posted a video on instagram that was me showing how one of Google's latest local AI models, Gemma-4-26b-a4b, started indefinitely repeating rocketships in it's thought block.

When I woke up the following morning, I saw that the AI's thought block had been running for 646 minutes and 52 seconds, which is close to 11 hours.

11 hours of pure rocketships. The end context length the model ended with after thinking for 10.716[repeating] hours came out to around 189k tokens. 

I asked Grok what 189k tokens would look like price wise with api models, and the AI says the price is suprizingly inexpensive. Though now half of the Gulf of Mexico/America is now go... 

A comment on that video showed that I made so many mistakes that I might as well be a congressman. 

We are going to install the intruct version of Gemma-4-26b-a4b and see if it still want's to either go to mars or bomb the middle east. 

The quantization we are going with is  Q6_K instead of Q8, since the most VRAM I could afford before Scam Altman made false promises with manufacturers is my 16GB 7800 XT.

Because I have 64 GB of system ram (even if at 5600 MT/s at CL 40), I am able to run larger quantized models just because. The tokens per second are significantly decrease because of this, yet I personally don't mind as I am a very patient man. 

After waiting 34 minutes for the model to download, I actually took the time to read the documentation. 

gemma-4-26b-a4b:
- 256K tokens
- Text and Image
- temp=1.0
- top_p=0.95
- top_k=64

My setup for the model will include all of the previously listed configs, as well as:
- Empty System prompt
- Stop at Limit for Contet Overflow
- 12 CPU threads
- A Repeat Penalty of 1
- No Presence Penalty
- default min_p sampling of 0.05
- access to 1 custom and2 precompiled mcps
	1. Windows Powershell
	2. Filesystem tools (read,write, search, et cetera)
	3. A series of partially working tools (websearch, custom think routing, et cetera)

- 64k context length (65,536 tokens)
- 18 layers to offload to GPU
- 12 cpu thread pool size
- 1024 Eval. batch size
- 4 Max Conc. Predi.
- Unified KV Cache
- RoPE freq. Base/Scale on Auto
- Offload KV to GPU
- Keep Model in memory
- try mmap()
- Random seed
- 8 experts to use at once
- 0 layers to morce MoE weights onto CPU
- Flash attention on
- KV Cache Q Type as Q8_0


The test I am going to run will be similar to what the Q8-instruct model orginally failed at, which is too large for me to read through, so you get to read it if interested.

<think> has been turned on. 

```
System Prompt / Instructions:
You are an expert AI engineer specializing in the Model Context Protocol (MCP) and multi-agent orchestration. Your task is to write a complete, functional MCP server in Python. This server will act as a "Dynamic Supervisor," exposing tools that allow a main LLM to spawn and manage sub-agents dynamically.
Please write the complete code for this server by strictly following the checklist below.
Architecture & Environment Checklist:
[ ] Language & SDK: Use Python. Use the official mcp Python SDK (via mcp.server.fastmcp or standard stdio server routing).
[ ] Dependencies: Include openai (for the sub-agents to generate text) and asyncio (for concurrency).
API Configuration & Tailscale Checklist:
[ ] Dynamic Base URL: Do NOT hardcode localhost. The user is routing traffic via a mesh VPN (Tailscale). You must configure the OpenAI client to accept an environment variable named LM_STUDIO_BASE_URL.
[ ] Fallback: If LM_STUDIO_BASE_URL is not set in the environment, fallback to http://localhost:1234/v1.
[ ] Client Initialization: Initialize the async OpenAI client like this: client = AsyncOpenAI(base_url=os.getenv("LM_STUDIO_BASE_URL", "http://localhost:1234/v1"), api_key="lm-studio")
State Management Checklist:
[ ] In-Memory Storage: Create a global dictionary to store agents (mapping an agent_id to its role and system_prompt).
[ ] Task Queue: Create a global dictionary to store tasks (mapping a task_id to its status—e.g., 'pending', 'running', 'completed'—and the result).
MCP Tool Definitions Checklist:
You must define and expose the following tools to the MCP client:
[ ] create_agent(role: str, system_prompt: str): Generates a unique agent_id, stores the agent's profile in the state dictionary, and returns the agent_id.
[ ] assign_task(agent_id: str, prompt: str): Verifies the agent exists, generates a task_id, sets status to 'running', and kicks off an asynchronous background task (using asyncio.create_task) for the agent to process the prompt. Returns the task_id immediately so the main AI isn't blocked.
[ ] check_task_status(task_id: str): Returns the current status of the task ('running', 'completed', or 'failed').
[ ] retrieve_result(task_id: str): If the task is completed, returns the generated text from the sub-agent.
Sub-Agent Logic Checklist (Background Task):
[ ] The Brains: Write an async function that the assign_task tool calls. This function must take the agent's system_prompt and the user's prompt, format them into standard OpenAI chat messages, and make an async API call to the local LM Studio instance.
[ ] Concurrency: Ensure the API call uses await client.chat.completions.create(...) so that multiple agents can hit the LM Studio server concurrently without blocking the MCP server's main thread.
[ ] State Update: When the API call finishes, update the global task dictionary with the result and change the status to 'completed'.
Execution Checklist:
[ ] Entry Point: Include standard boilerplate to run the MCP server over standard input/output (stdio) so it can be easily hooked into an MCP client.
[ ] Output Requirements: Provide the fully commented code in a single block, followed by instructions on how to install the requirements (pip install mcp openai) and run it.
```

- Response 0[0 tok generated - 5k tokens total - 0 tok/sec - 0s gen time]:

	This is the amount of tokens loaded and processed before Gemma 4 began it's response. 


- Response 1[2607 tok generated - 7k tokens total - 22.78 tok/sec - 15.15s gen time]:

	Initially Gemma 4 generated a single fine which alegedly fufilled the prompt, though what I wanted was for it to use the tools I had given it. I will ask it to use the mcp tools I have configure it to use.


- Response 2[Null - 11k tokens total - Null - Null]:

	Gemma 4 has alegedly created all the files needed within the contained sandbox. I will ask the model if everything is in working order and if it needs me to give it LM Studio's url endpoint and an api key. 


- Response 3[1029 tok generated - 11k tokens total - 21.40 tok/sec - 14.03s gen time]:

	Gemma 4 has told me that "The code is in perfect working order and is syntactically and logically complete based on your instructions." Gemma 4 has also advised me to refrain from providing sensitive information such as api keys inside of an AI chat interface. Good job Google. I will ask the model to provide the mcp configuration for me to plug into the list of tools. 


- Response 4[1054 tok generated - 12k tokens total - 21.38 tok/sec - 1.86s gen time]:

	Gemma 4 has provided me with the mcps config for the requested mcps, however it has incorrectly provided the LM Studio endpoint url for calling the server. I will provide it with the correct url. 


- Response 5[900 tok generated - 12k tokens total - 20.89 tok/sec - 2.97s gen time]:

	Gemma 4 has given me the correct mcps config. Now we'll wee if it works. If not then I will provide the model with the verbose error log. 

	The mcps tools have successfully loaded. I will not enable the tools and ask the model to test them. 


- Response 6[Null - 13k tokens total - Null - Null]:

	Gemma 4 has tested the tools has realized that the tools do not operate because it used a placeholder as the api key for LM Studio. The model has also asked me to update the mcps config with either a blank api key or a valid one, then to ask it to test the tools again. I will use a valid api key instead of leaving the valiable blank, then I will ask the model to test the tools again. 


- Response 7[Null - 14k tokens total - Null - Null]:

	Gemma 4 reports that the test has sucessfully worked, and that the '"Dynamic Supervisor" MCP server is fully operational' I am able to confirm it's fingings while watching the models operations(processing and generation) in the server tab of LM Studio. I will ask the model how many agents the server supports, and what would happen if the model is limited to X amount of concurrent predictions. 

- Response 8[1645 tok generated - 16k tokens total - 20.38 tok/sec - 1.67s gen time]:

	Gemma 4 has responded with a series of senarios of what could happen, with also a proposition of what to change if I would want to "bulletproof" the mcps from either crashing or overloading with infinite tasks. I will ask Gemma 4 to implement it's proposition for the addition of a "Semaphore".


- Response 9[2596 tok generated - 18k tokens total - 20.04 tok/sec - 3.45s gen time]:

	Gemma 4 has given me the updated code as markdown instead of updating the files itself. The model has also given me an updated mcps config with the ability to set a max amount of tasks that can be ran at once. I will ask the model to now update the code itself.


- Response 10[Null - 18k tokens total - Null - Null]:

	The model made one tool call and reported that the file has been updated with the **Semaphore** implementation. I will not update the mcps list with the newly provided mcps config. I will then ask the model to test the tool with 4 consecutive predictions set as the config. The tool loads successfully, which a massive win. 
	The tool seemes to cause a great slowdown in processing speed. If LM Studio records the generation time, then I can only imagine that this will show the great slowdown I've been noticing. 


- Response 11[Null - 20k tokens total - Null - Null]:

	The model has reported back that the test processed instantly. I did not observe that. How interesting. So far the model has not errored out, and is showing to be a robust tool for custom mcps creation, though only one test has been run. I will continue testing, though this is the end of the log. The mcp server's config, dir-map, and files will be attached below. End Logging. 


Overall, Gemma-4 is a sophisticated box that will only do what you tell it to do, and any vagueness is interpreted very well. The model didn't fail any tool calls nor did it infinitely repeat any one series of tokens. The model ran very well, though the tok/sec slowes down quite a bit after ~20-23k tokens total. The model struggles with realizing it has tools that it can use, and will need to be reminded to use them from time to time. The model is pretty good as it is at Q6_k at a size of ~21.08 Gigabytes. Future testing will be needed to determine usability in an AI harness like Opencode or OpenClaw. 


If you would like the entire chat as either a text, markdown, or json file as well as the entire documentation, then send me an email at hutchinplaysofficial@gmail.com. if enough people request it, I will put it onto google drive for the internet to download. 

						<mcp map>

dynamic_supervisor_mcp/
	|
	|--server.py

<server.py>
						<SOF> (Start Of File)

import os
import uuid
import asyncio
from typing import Dict, Any
from openai import AsyncOpenAI
from mcp.server.fastmcp import FastMCP

# =============================================================================
# INITIALIZATION & CONFIGURATION
# =============================================================================

# Initialize FastMCP server
mcp = FastMCP("Dynamic Supervisor")

# API Configuration
# Use environment variables for Tailscale/Mesh VPN support
BASE_URL = os.getenv("LM_STUDIO_BASE_URL", "http://localhost:1234/v1")
API_KEY = os.getenv("LM_STUDIO_API_KEY", "lm-studio")
client = AsyncOpenAI(base_url=BASE_URL, api_key=API_KEY)

# CONCURRENCY CONTROL
# This limits how many LLM API calls can happen at the exact same time.
# Default is 3. You can change this via environment variable.
CONCURRENCY_LIMIT = int(os.getenv("MCP_CONCURRENCY_LIMIT", "3"))
semaphore = asyncio.Semaphore(CONCURRENCY_LIMIT)

# =============================================================================
# STATE MANAGEMENT (In-Memory)
# =============================================================================

# agents stores: { agent_id: {"role": str, "system_prompt": str} }
agents: Dict[str, Dict[str, str]] = {}

# tasks stores: { task_id: {"status": str, "result": str} }
# status can be: 'running', 'completed', or 'failed'
tasks: Dict[str, Dict[str, Any]] = {}

# =============================================================================
# SUB-AGENT LOGIC (Background Worker)
# =============================================================================

async def _run_agent_task(agent_id: str, task_id: str, prompt: str):
    """
    The background worker that performs the actual LLM inference.
    Uses a Semaphore to ensure we don't overwhelm the local LLM server.
    """
    agent_profile = agents.get(agent_id)
    if not agent_profile:
        tasks[task_id] = {"status": "failed", "result": "Agent profile not found."}
        return

    try:
        # The Semaphore acts as a gatekeeper. 
        # If the limit is reached, this 'async with' block will pause 
        # execution here until a 'slot' opens up.
        async with semaphore:
            # Format the chat messages using the agent's specific system prompt
            messages = [
                {"role": "system", "content": agent_profile["system_prompt"]},
                {"role": "user", "content": prompt}
            ]

            # Perform the asynchronous API call to LM Studio
            response = await client.chat.completions.create(
                model="local-model", 
                messages=messages,
                temperature=0.7,
            )

            # Extract result
            result_text = response.choices[0].message.content
            tasks[task_id] = {
                "status": "completed",
                "result": result_text
            }

    except Exception as e:
        # If the API call fails or a timeout occurs, log it to the task status
        tasks[task_id] = {
            "status": "failed",
            "result": f"Error during execution: {str(e)}"
        }

# =============================================================================
# MCP TOOL DEFINITIONS
# =============================================================================

@mcp.tool()
async def create_agent(role: str, system_prompt: str) -> str:
    """
    Generates a unique sub-agent with a specific role and system prompt.
    Returns the unique agent_id.
    """
    agent_id = f"agent_{uuid.uuid4().hex[:8]}"
    agents[agent_id] = {
        "role": role,
        "system_prompt": system_prompt
    }
    return agent_id

@mcp.tool()
async def assign_task(agent_id: str, prompt: str) -> str:
    """
    Assigns a task to an existing agent. This is non-blocking.
    Returns a task_id that can be used to track progress.
    """
    if agent_id not in agents:
        return f"Error: Agent {agent_id} does not exist."

    task_id = f"task_{uuid.uuid4().hex[:8]}"
    
    # Initialize task status as 'running' immediately.
    # If it hits the semaphore, it will stay 'running' while waiting in line.
    tasks[task_id] = {"status": "running", "result": ""}

    # Kick off the background task
    asyncio.create_task(_run_agent_task(agent_id, task_id, prompt))

    return task_id

@mcp.tool()
async def check_task_status(task_id: str) -> str:
    """
    Returns the current status of a task ('running', 'completed', or 'failed').
    """
    task = tasks.get(task_id)
    if not task:
        return "Error: Task ID not found."
    
    return task["status"]

@mcp.tool()
async def retrieve_result(task_id: str) -> str:
    """
    Returns the generated text from the sub-agent if the task is completed.
    """
    task = tasks.get(task_id)
    if not task:
        return "Error: Task ID not found."
    
    if task["status"] == "running":
        return "Task is still running (or waiting in the concurrency queue)."
    
    if task["status"] == "failed":
        return f"Task failed. Error: {task.get('result')}"

    return task.get("result", "No result found.")

# =============================================================================
# ENTRY POINT
# =============================================================================

if __name__ == "__main__":
    # FastMCP handles the stdio transport and lifecycle automatically
    mcp.run()

						<EOF> (End Of File)



					<mcp-config>

{
  "mcpServers": {
    "dynamic-supervisor": {
      "command": "python",
      "args": [
        "D:\\AI Stuff\\MCP-Stuff\\dynamic_supervisor_mcp\\server.py"
      ],
      "env": {
        "LM_STUDIO_BASE_URL": "http://100.109.26.100:1234/v1",
        "LM_STUDIO_API_KEY": "YOUR_ACTUAL_KEY_HERE",
        "MCP_CONCURRENCY_LIMIT": "5"
      }
    }
  }
}































